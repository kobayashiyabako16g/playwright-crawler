{
  "name": "playwright-crawler",
  "version": "1.0.0",
  "description": "",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build:crawler": "dotenvx run -- turbo run build --filter=@apps/crawler...",
    "dev:crawler": "dotenvx run -- turbo run dev --filter=@apps/crawler... --ui=tui",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "env:set": "dotenvx set",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "turbo": "^2.8.21",
    "oxfmt": "^0.42.0",
    "oxlint": "^1.57.0",
    "@dotenvx/dotenvx": "^1.58.0"
  }
}
